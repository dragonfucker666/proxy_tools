# Send TCP

is a tool for sending all connections incoming to the `$INPUT` Unix file socket to some host and port

Arguments:

```
--ipv [4|6|any] [host] [port]
```

the `--ipv` flag (for choosing the IP version) is optional and defaults to "any"
