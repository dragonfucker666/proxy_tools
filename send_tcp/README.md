# Send TCP

is a tool for sending all connections incoming to the `$INPUT` Unix file socket to some host (first argument; may be a domain name, prefixed with "domain:", or an ipv4/ipv6 address, prefixed with "ipv4:" or "ipv6:" respectively) and port (second argument)
