# Listen TCP

is a tool for listening on some host (first argument; may be a domain name, prefixed with "domain:", or an ipv4/ipv6 address, prefixed with "ipv4:" or "ipv6:" respectively) and port (second argument) and proxying all the connections into the `$OUTPUT` Unix file socket (`$OUTPUT` contains the path to the socket)
